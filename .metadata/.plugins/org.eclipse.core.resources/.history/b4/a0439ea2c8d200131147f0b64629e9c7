{% if tweets.count > 0 %}

{% for tweet in tweets %}
    <li><a href="{{ article.object.get_absolute_url }}">{{ article.object.title }}</a></li>
{% endfor %}

{% else %}

<li>None to show!</li>

{% endif %}